// Placeholder test. Real tests (movement determinism, serialization
// round-trips, malformed-packet fuzzing, TCP frame reassembly — README §10)
// are added once the corresponding shared/ code is implemented.

#include <cassert>

int main() {
    assert(1 + 1 == 2);
    return 0;
}
